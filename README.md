# BNUCGwork2
## 1. 实验目标
通过本次实验，你将能够：
1. 深入理解 3D 空间中的坐标变换流程（模型-视图-投影/MVP(Model-View-Projection)变换）。
2. 独立推导并用代码实现模型变换（Model）、视图变换（View）和投影变换（Projection）矩阵。
3. 掌握面向数据编程框架 Taichi 的基本语法与矩阵操作。
## 2. 实验背景
在提供的代码框架中，我们定义了一个三维空间中的三角形。该三角形的初始顶点坐标为：
$$v_0$$: (2.0, 0.0, -2.0)
$$v_1$$: (0.0, 2.0, -2.0)
$$v_2$$: (-2.0, 0.0, -2.0)
你需要将这三个三维空间中的点，通过 MVP 矩阵 变换为二维屏幕坐标，并在 Taichi 的 GUI 窗口中绘制出对应的线框三角形。
## 3. 实验要求
你需要补全代码框架中的三个核心函数，返回对应的 $$4 \times 4$$ 齐次坐标变换矩阵：
1. get_model_matrix(angle):
  - 功能: 接收一个旋转角度（角度制），返回绕 Z 轴旋转该角度的模型变换矩阵。
2. get_view_matrix(eye_pos):
  - 功能: 接收相机位置（三维向量），返回视图变换矩阵。你需要将相机平移至世界坐标系的原点。
3. get_projection_matrix(eye_fov, aspect_ratio, zNear, zFar):
  - 功能: 接收视场角（Y 轴方向，角度制）、屏幕长宽比、近截面距离和远截面距离，返回透视投影矩阵。
  - 流程: 先将透视平截头体挤压为正交长方体（透视到正交矩阵$$M_{persp \to ortho}$$），再进行正交投影（$$M_{ortho}$$）。
## 4. 实验效果
<img width="1116" height="718" alt="image" src="https://github.com/user-attachments/assets/becddbf0-9db2-4726-b8a6-11c23ff83944" />


- 实验在一个纯黑色的视口中运行，画面中央成功渲染了一个由独立线条构成的线框三角形。
- 三角形的三条边被精确地赋予了不同RGB色彩，这通常用于调试顶点颜色或验证渲染管线的着色器逻辑：
- 按下键盘“A”“D”键可以控制三角形左右旋转
## 5. 附加实验
要求：
- 构建 3D 立方体并进行透视旋转：
  目前我们的程序中只绘制了一个扁平的三角形。为了真正展现视图变换的三维空间感，大家可以尝试将二维的三角形升级为真正的三维立体几何体。
效果：
<img width="992" height="784" alt="image" src="https://github.com/user-attachments/assets/6e4ee604-1aa4-429d-959c-6ac819b003a7" />

如图所示，我们基本完成了附加实验的要求内容。在这个版本中，我们直接使用了 Taichi 提供的 ti.Matrix，并在 Kernel 内部用 @ 操作符完成了MVP矩阵的连乘与变换。且借助于 gui.lines() 方法，我们可以直接向 GUI 喂送两组起点和终点的 numpy 数组来完成全部 12 条边的渲染，避免了 Python 层的 for 循环，性能更佳。
## 6. 一些其他的想法：
我将这个绿色线框改造成了一个带彩色实面的立方体进行渲染。在kernel中遍历六个面，找到每个面的四个顶点进行MVP变换，并且引入画家算法：
- 在 Kernel 中，我们计算了每个面全部 4 个顶点的裁剪坐标（Clip Space）的 $z$ 分量，并取平均值 faces_depth[f_idx] 作为深度。
- 在主循环的 Numpy 阶段，我们使用 np.argsort(depths)[::-1] 来按深度从远到近给面排序。
- 绘制时，按这个顺序优先绘制远处的面，这样近处的面就会自动覆盖在上面，实现了实体的视觉效果。
<img width="1096" height="860" alt="image" src="https://github.com/user-attachments/assets/34b94cef-4f45-4926-b900-0f74cd5f526e" />

